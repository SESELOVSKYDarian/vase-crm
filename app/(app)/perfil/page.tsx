"use client";
import { useEffect, useState } from "react";
import { Card } from "@/components/ui/card";
import { Input, Label } from "@/components/ui/input";
import { Button } from "@/components/ui/button";
export default function PerfilPage() { const [user, setUser] = useState<any>(null); const [password, setPassword] = useState(""); const [message, setMessage] = useState(""); useEffect(() => { fetch("/api/auth/me").then((r) => r.json()).then((d) => setUser(d.user)); }, []); return <div className="max-w-2xl space-y-6"><div><h1 className="text-xl font-bold">Mi perfil</h1><p className="text-sm text-muted-foreground">Tus datos y acceso al sistema.</p></div><Card className="space-y-4 p-5"><div><Label>Nombre</Label><p className="text-sm font-medium">{user?.name ?? "Cargando…"}</p></div><div><Label>Email</Label><p className="text-sm font-medium">{user?.email ?? "Cargando…"}</p></div><div><Label>Roles</Label><p className="text-sm font-medium">{user?.roles?.join(", ") || user?.role || "—"}</p></div></Card><Card className="space-y-4 p-5"><h2 className="font-semibold">Cambiar contraseña</h2><div><Label htmlFor="new-password">Nueva contraseña</Label><Input id="new-password" type="password" minLength={8} value={password} onChange={(e) => setPassword(e.target.value)} /></div><Button onClick={() => setMessage("La actualización de contraseña quedará disponible al conectar la API de perfil.")} disabled={password.length < 8}>Actualizar contraseña</Button>{message && <p className="text-sm text-vase-green">{message}</p>}</Card></div>; }
